# Flask
SECRET_KEY=changeme

# MySQL Database
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=changeme
DB_NAME=maternal_health

# --- Phase 2 --- Email (Gmail SMTP, used to send patient access codes)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=changeme@gmail.com
EMAIL_PASSWORD=changeme

# --- Phase 3 --- MQTT (self-hosted Mosquitto broker, local dev = no TLS)
MQTT_HOST=localhost
MQTT_PORT=1883
